# Porous Media Parameter Calculator for Annular Finned Tubes

**Annular finned tube heat exchangers → CFD porous media parameters**

Nir (1991) correlation을 사용하여 annular fin 형상에서 CFD porous zone 파라미터 (점성 저항 1/K, 관성 저항 C₂)를 계산합니다.

---

## 🚀 빠른 시작

### 방법 1: Streamlit 웹 앱 (권장)

```bash
# 의존성 설치 (처음 한 번만)
pip install -r requirements.txt

# 웹 앱 실행
streamlit run streamlit_app.py
```

브라우저가 자동으로 열립니다 (`http://localhost:8501`)

**주요 기능:**
- 슬라이더로 실시간 파라미터 조정 (핀 간격, 핀 높이, 온도, 속도)
- 즉시 계산 및 시각화
- 핵심 수식 표시 (LaTeX)
- 결과 다운로드 (PNG, TXT, JSON)
- 영어 인터페이스

### 방법 2: CLI (Command Line)

```bash
# 기본 실행
python cfd_porous_calc.py --Fs 4.0 --hf 4.0 --T 14.8 --v 2.0

# 대화형 입력 모드
python cfd_porous_calc.py --interactive

# 고급 옵션 (피팅 범위 변경)
python cfd_porous_calc.py --Fs 4.0 --hf 4.0 --T 14.8 --v 2.0 \
    --v_min 0.5 --v_max 3.5 --n_points 100
```

**출력 파일:**
- `Fs4.0_hf4.0_T14.8_v2.00.png`: 피팅 결과 시각화
- `Fs4.0_hf4.0_T14.8_v2.00.txt`: 계산 결과 텍스트 파일

### 방법 3: Python API

```python
from cfd_porous_calc import calculate_porous_parameters

result = calculate_porous_parameters(
    Fs_mm=4.0,              # 핀 간격 [mm]
    hf_mm=4.0,              # 핀 높이 [mm]
    T_celsius=14.8,         # 온도 [°C]
    v_design=2.0,           # 설계 풍속 [m/s]
    v_range=(0.5, 3.5),     # 피팅 속도 범위 [m/s]
    n_points=50             # 피팅 점 개수
)

# CFD 입력 파라미터 출력
print(f"1/K = {result['porous']['inv_K']:.4e} [1/m²]")
print(f"C₂ = {result['porous']['C2']:.4f} [1/m]")
print(f"R² = {result['porous']['R_squared']:.6f}")
```

---

## 📊 계산 과정

```
┌─────────────────────────────────────────────────────────────┐
│  INPUT: Fs, hf, T, v                                        │
│  (핀 간격, 핀 높이, 온도, 설계 속도)                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 1: 공기 물성치 계산                                     │
│    ρ, μ, k, Pr (Sutherland's Law)                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 2: 기하 파라미터 계산                                   │
│    Do = Dc + 2×hf                                           │
│    Fp = Fs + δf                                             │
│    ε = Fs / (Fs + δf)                                       │
│    σ = (S1 - Dc - 2×hf×δf/Fp) / S1                         │
│    AR = A_total / A_bare (면적비)                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 3: 다중점에서 압력강하 계산 (0.5~3.5 m/s, 50점)        │
│                                                             │
│    FOR v in [0.5, 0.56, ..., 3.5]:                         │
│      v_max = v / σ                                          │
│      Re_Dc = ρ × v_max × Dc / μ                            │
│      f_N = Nir(Re_Dc, S1/Dc, AR)                           │
│      ΔP/L = N × f_N × ρ × v_max² / (2 × S2)                │
│                                                             │
│    → 50개 (v_i, Y_i) 데이터 쌍 생성                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 4: 최소제곱 피팅 (Least Squares)                       │
│                                                             │
│    목표: Y ≈ A×v + B×v²  (Darcy-Forchheimer 형태)          │
│                                                             │
│    행렬 방정식:                                              │
│      [v₁  v₁²]             [Y₁]                            │
│      [v₂  v₂²]   [A]       [Y₂]                            │
│      [ ⋮   ⋮ ] × [B]   =   [ ⋮]                            │
│      [v₅₀ v₅₀²]            [Y₅₀]                           │
│                                                             │
│    해법: (X^T X) [A B]^T = X^T Y                            │
│    품질: R² > 0.999 (매우 정확)                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  STEP 5: Porous 파라미터 변환                                │
│                                                             │
│    Darcy-Forchheimer:  ΔP/L = (μ/K)×v + (C₂×ρ/2)×v²       │
│    Fitting 결과:       ΔP/L = A×v + B×v²                   │
│                                                             │
│    따라서:                                                  │
│      1/K = A / μ        [1/m²] ← CFD 점성 저항              │
│      C₂ = 2×B / ρ       [1/m]  ← CFD 관성 저항              │
│      K = μ / A          [m²]   ← 투과도                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  OUTPUT: 1/K, C₂, ε, a_fs  → ANSYS Fluent 입력             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📐 핵심 수식

### Nir (1991) 마찰계수 상관식

```
f_N = 1.1 × Re_Dc^(-0.25) × (S1/Dc)^(-0.4) × AR^(0.15)
```

여기서:
- `Re_Dc`: 튜브 외경 기준 Reynolds 수
- `S1/Dc`: 횡방향 피치 비율
- `AR`: 면적비 (A_total / A_bare)

### Darcy-Forchheimer 방정식

```
ΔP/L = (μ/K) × v + (C₂×ρ/2) × v²
       ─────────   ───────────────
       점성 저항      관성 저항
```

### 최소제곱 피팅

```
minimize: Σ(Y_i - A×v_i - B×v_i²)²

해: (X^T X) [A]  = X^T Y
            [B]
```

---

## 🔧 코드 구조

```
cfd_porous_calc.py
├── air_properties()              # 공기 물성치 (Sutherland's Law)
├── AnnularFinGeometry            # 기하 파라미터 계산 클래스
│   ├── __post_init__()           # Do, Fp, ε, σ, AR 계산
│   └── _calculate_areas()        # 면적비 계산
├── nir_friction_factor()         # Nir (1991) 마찰계수
├── calculate_pressure_drop()     # 압력강하 계산
├── fit_darcy_forchheimer_multipoint()  # 다중점 최소제곱 피팅 ★
├── briggs_young_heat_transfer()  # 열전달 계수
├── calculate_porous_parameters() # 메인 계산 함수 ★
├── plot_fitting_results()        # 시각화
├── save_results_to_txt()         # 텍스트 파일 저장
└── print_results()               # 콘솔 출력

streamlit_app.py                  # 웹 앱 UI
├── 사이드바: 입력 파라미터 슬라이더
├── 탭1: 주요 결과 (CFD 파라미터)
├── 탭2: 피팅 분석 (그래프)
├── 탭3: 상세 데이터 (테이블)
└── 탭4: 내보내기 (PNG/TXT/JSON)
```

---

## ⚙️ 입력 파라미터

### 필수 파라미터

| 파라미터 | 기호 | 단위 | 범위 | 기본값 | 설명 |
|---------|-----|------|------|--------|------|
| 핀 간격 | Fs | mm | 0.5 ~ 30 | 4.0 | 인접한 핀 사이의 거리 (일반: 1-15mm) |
| 핀 높이 | hf | mm | 0.5 ~ 50 | 4.0 | 핀의 반경 방향 높이 (일반: 3-20mm) |
| 온도 | T | °C | -50 ~ 100 | 14.8 | 공기 온도 (일반: -40 ~ 60°C) |
| 설계 속도 | v | m/s | 0.1 ~ 20 | 2.0 | 입구 superficial velocity (일반: 0.5-10 m/s) |

### 고급 파라미터 (조정 가능)

| 파라미터 | 기호 | 단위 | 범위 | 기본값 | 설명 |
|---------|-----|------|------|--------|------|
| 튜브 외경 | Dc | mm | 5 ~ 100 | 24.0 | 일반: 10-50mm |
| 핀 두께 | δf | mm | 0.05 ~ 5 | 0.5 | 일반: 0.1-2mm |
| 횡방향 피치 | S1 | mm | 10 ~ 300 | 55.333 | > Dc + 2×hf 필요 |
| 피치 비율 | s1/s2 | - | 0.1 ~ 10 | 1.0 | 횡방향/종방향 피치 비율 |
| 튜브 열 수 | N | - | 1 ~ 50 | 4 | 일반: 1-20열 |

### 피팅 옵션

| 옵션 | 범위 | 기본값 | 설명 |
|-----|------|--------|------|
| v_min | 0.01 ~ 30 m/s | 0.5 m/s | 피팅 최소 속도 |
| v_max | 0.1 ~ 30 m/s | 3.5 m/s | 피팅 최대 속도 |
| n_points | 5 ~ 500 | 50 | 피팅 점 개수 |

> **참고:** 모든 파라미터는 step=0.01 (소수점 2자리 단위)로 조정 가능

---

## 📤 출력 파라미터

### CFD 입력값 (핵심)

| 파라미터 | 기호 | 단위 | ANSYS Fluent 이름 |
|---------|-----|------|-------------------|
| 점성 저항 | 1/K | 1/m² | Viscous Resistance |
| 관성 저항 | C₂ | 1/m | Inertial Resistance |
| 공극률 | ε | - | Porosity |
| 비표면적 | a_fs | 1/m | Surface Area Density |
| 열전달계수 | h_fs | W/(m²·K) | Fluid-Solid HTC |

### 피팅 품질

| 지표 | 설명 | 목표값 |
|-----|------|--------|
| R² | 결정계수 | > 0.999 |
| Residual Std Dev | 잔차 표준편차 | < 1% of mean |

---

## 🖥️ CFD 소프트웨어 설정

### ANSYS Fluent

**Porous Zone 설정:**

1. **Cell Zone Conditions** → Porous Zone 체크

2. **Momentum**:
   - Viscous Resistance (1/m²):
     - Direction-1 (유동 방향): `1/K` 값 입력
     - Direction-2, 3 (횡방향): `1/K × 1000` (유동 차단)

   - Inertial Resistance (1/m):
     - Direction-1 (유동 방향): `C₂` 값 입력
     - Direction-2, 3 (횡방향): `C₂ × 1000` (유동 차단)

3. **Porosity**: `ε` 값 입력

4. **Energy** (LTNE 모델 사용 시):
   - Non-Equilibrium Thermal Model 활성화
   - Surface Area Density: `a_fs` 값 입력
   - Fluid-Solid Heat Transfer Coefficient: `h_fs` 값 입력

### SimScale / COMSOL

- **Permeability (K)**: `K = 1/(1/K)` 계산하여 입력 [m²]
- **Forchheimer Coefficient**: `C₂` 값 직접 입력 [1/m]

---

## 📊 예시 결과

### 입력 조건
```
Fs = 4.0 mm
hf = 4.0 mm
T = 14.8 °C
v = 2.0 m/s
```

### 출력 (CFD 파라미터)
```
1/K = 1.1379e+05  [1/m²]
C₂  = 4.6136      [1/m]
K   = 8.7882e-06  [m²]
ε   = 0.8889      [-]
a_fs= 666.67      [1/m]
R²  = 0.9997      (excellent fit)
```

### 설계점 성능
```
Re_Dc = 5980
ΔP_total = 3.44 Pa
h_fs = 59.26 W/(m²·K)
```

---

## 📈 피팅 정확도

**다중점 최소제곱 피팅 (0.5~3.5 m/s, 50점)**

- Reynolds 수 범위: 1,495 ~ 10,465
- 결정계수 (R²): > 0.999
- 전체 속도 범위에서 균일한 정확도

**장점:**
- Nir 상관식의 v^1.75 거동을 Darcy-Forchheimer 2차식으로 정확히 근사
- 저속~고속 전 영역에서 신뢰성 높은 결과

---

## 🔬 참고문헌

1. **Nir, A.** (1991). "Heat Transfer and Friction Factor Correlations for Crossflow over Staggered Finned Tube Banks", *Heat Transfer Engineering*, Vol.12, No.1, pp.43-58.

2. **Briggs, D.E., & Young, E.H.** (1963). "Convection heat transfer and pressure drop of air flowing across triangular pitch banks of finned tubes."

---

## 📋 명령줄 옵션 전체 목록

```bash
python cfd_porous_calc.py [OPTIONS]

필수 옵션 (또는 --interactive 사용):
  --Fs FLOAT        핀 간격 [mm]
  --hf FLOAT        핀 높이 [mm]
  --T FLOAT         대기 온도 [°C]
  --v FLOAT         설계 풍속 [m/s]

선택 옵션:
  --Dc FLOAT        튜브 외경 [mm] (기본: 24.0)
  --delta_f FLOAT   핀 두께 [mm] (기본: 0.5)
  --s1 FLOAT        횡방향 피치 [mm] (기본: 55.333)
  --pitch_ratio     s1/s2 비율 (기본: 1.0)
  --N INT           튜브 열 수 (기본: 4)
  --v_min FLOAT     피팅 최소 속도 [m/s] (기본: 0.5)
  --v_max FLOAT     피팅 최대 속도 [m/s] (기본: 3.5)
  --n_points INT    피팅 점 개수 (기본: 50)

모드 옵션:
  --interactive     대화형 입력 모드
  --no-plot         시각화 생성 안 함
  -h, --help        도움말 표시
```

---

## 💡 Tips

### 1. 피팅 범위 선택
- 기본값 (0.5~3.5 m/s)은 일반적인 공랭식 열교환기에 적합
- 고속 응용: `--v_max` 증가 (예: 5.0 m/s)
- 저속 응용: `--v_min` 감소 (예: 0.3 m/s)

### 2. 피팅 점 개수
- 기본 50점: 정확도와 계산 속도의 균형
- 높은 정확도 필요: `--n_points 100`
- 빠른 계산: `--n_points 30`

### 3. Streamlit 앱 활용
- 여러 설계안을 빠르게 비교할 때 유용
- 슬라이더로 민감도 분석 가능
- Export 탭에서 결과 다운로드

---

## 📝 버전 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| 4.1 | 2026-01-26 | 슬라이더 범위를 물리적 타당 범위로 확장, step=0.01로 정밀 조정 가능 |
| 4.0 | 2026-01-17 | Streamlit 웹 앱 추가, TXT 파일 자동 저장, 영어 인터페이스 |
| 3.0 | 2026-01-17 | 다중점 최소제곱 피팅 (0.5~3.5 m/s, 50점), CLI 인터페이스 |
| 2.0 | 2026-01-17 | Nir (1991) 상관식 기반으로 마이그레이션 |
| 1.0 | 2025-XX-XX | 초기 버전 (Wang 상관식) |

---

**작성자:** Claude (Anthropic)
**최종 업데이트:** 2026-01-26
**라이선스:** Educational & Research Use

